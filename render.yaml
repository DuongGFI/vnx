services:
  - type: web
    name: my-scraper
    env: docker
    dockerfilePath: ./Dockerfile
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONUNBUFFERED
        value: "1"
